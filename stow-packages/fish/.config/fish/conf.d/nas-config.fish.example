# NAS Configuration Example
# Copy this file to nas-config.fish and update with your actual values
# DO NOT commit the actual nas-config.fish file to git!

# NAS Backup configuration
set -g NAS_BACKUP_HOST "your-nas-backup-hostname-or-ip"
set -g NAS_BACKUP_SHARE "your-backup-share-name"

# NAS Media configuration  
set -g NAS_MEDIA_HOST "your-nas-media-hostname-or-ip"
set -g NAS_MEDIA_SHARE "your-media-share-name"